import 'package:flutter/material.dart';

class NavigationGame extends StatelessWidget {
  const NavigationGame({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child:Text("Games"));
  }
}
